<%= render 'shared/errors', obj: @message %>

<%= form_with(model: @message, local: true, data: { turbo: false }) do |f| %>    
    <p>
        <%= f.label :body %>
        <br />
        <%= f.text_area :body %>
    </p>    
    <p>
        <%= f.submit %>
    </p>

<% end %>
